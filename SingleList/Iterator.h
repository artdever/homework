#pragma once

#include"Node.h"

template<class T>
class Iterator {

public:

	//T& operator*();  // dereferencing operator
	//Iterator& operator=(const iterator& rhs);
	//bool operator==(const iterator& rhs) const;
	//bool operator!=(const iterator& rhs) const;
	//iterator& operator++();
//	Iterator<T> operator++(int);
	//iterator& operator--();
	//iterator operator--(int);

protected:
	//Node<T>* current_node;
};

//template<class T>
//T& Iterator<T>::operator*() {
////	return current_node;
//}
//
//template<class T>
//Iterator<T> Iterator<T>::operator++(int v) {
//	Iterator<T>* temp = this;
//	temp += 1;
//	return *temp;
//}

